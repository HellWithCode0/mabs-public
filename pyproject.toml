[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mabs"
version = "4.0.1a1"
description = "MABS v4: CASCADE streaming QEC decoder (ExactPatternCache + clique MWPM)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Aryaman Katoch" }]
dependencies = [
    "stim",
    "pymatching",
    "numpy",
    "pytest",
    "matplotlib",
]

[project.optional-dependencies]
dev = ["pytest", "matplotlib"]

[project.scripts]
mabs = "mabs.__main__:main"

[project.urls]
Homepage = "https://github.com/HellWithCode0/mabs"
Repository = "https://github.com/HellWithCode0/mabs"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
