[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mabs"
version = "4.3.0a1"
description = "MABS v4.3: CASCADE FLASH with a certified CLUSTER route for K>=3 windows; 4.3.0a1"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Aryaman Katoch" }]
dependencies = [
    "stim",
    "pymatching",
    "numpy",
    "pytest",
    "matplotlib",
]

[project.optional-dependencies]
dev = ["pytest", "matplotlib"]

[project.scripts]
mabs = "mabs.__main__:main"

[project.urls]
Homepage = "https://github.com/HellWithCode0/mabs"
Repository = "https://github.com/HellWithCode0/mabs"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
